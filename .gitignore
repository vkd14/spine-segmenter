# =============================================================================
# spine-seg-app — gitignore
# =============================================================================

# --- Python ------------------------------------------------------------------
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg
*.egg-info/
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib64/
parts/
sdist/
var/
wheels/
pip-wheel-metadata/
share/python-wheels/
MANIFEST
.pytest_cache/
.mypy_cache/
.dmypy.json
.ruff_cache/
.tox/
.coverage
htmlcov/
.hypothesis/

# --- Virtual environment -----------------------------------------------------
.venv/
venv/
ENV/
env/

# --- Trained weights & pipeline outputs (too big for git) --------------------
# nnU-Net's 5 fold checkpoints — ~11 GB total. Download separately (see README).
nnUNet_results/
# Extra checkpoint copy.
segmenter_phase_c.pth
# Per-case pipeline outputs (NIfTI, STL, raw_prediction, etc.).
results/
# nnU-Net staging dirs created at inference time.
nnUNet_raw/
nnUNet_preprocessed/

# --- Node / pptx tooling ------------------------------------------------------
node_modules/
package-lock.json

# --- Build / LaTeX intermediates ----------------------------------------------
*.aux
*.log
*.toc
*.nav
*.snm
*.out
*.synctex.gz
*.fdb_latexmk
*.fls
*.bbl
*.blg

# --- IDE / editor / OS --------------------------------------------------------
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# --- Streamlit cache ----------------------------------------------------------
.streamlit/secrets.toml

# --- Misc ---------------------------------------------------------------------
*.pdf.aux
*.pkl
*.tmp
*.bak
